# 띄우는 법

코드는 다 됐습니다. 여기서부터는 저장소 설정과 사람 일입니다.

---

## 1. 배포 켜기 (5분)

저장소 **Settings → Pages → Source** 를 **GitHub Actions** 로 바꾸세요.
그러면 푸시할 때마다 `.github/workflows/pages.yml` 이

1. 자판 데이터를 libhangul 원본과 대조하고
2. 계산 골든 벡터 87건을 돌리고
3. 카카오톡 파서 테스트 91건을 돌리고
4. 색 대비가 WCAG AA를 만족하는지 계산해 보고

전부 통과하면 배포합니다. 하나라도 깨지면 배포하지 않습니다.

주소는 `https://<사용자이름>.github.io/<저장소이름>/` 입니다.

---

## 2. 수익화 — 순서가 중요합니다

전부 `assets/config.js` 한 파일에서 켭니다. **비워 두면 그 블록은 화면에 아예 나오지 않습니다.**

### 지금 바로

| 무엇 | 하는 일 |
|---|---|
| `sponsorUrl` | GitHub Sponsors / BuyMeACoffee 링크. 가장 마찰 없는 첫 수익 경로 |
| `contactEmail` | 문의 주소. 소개 페이지와 개인정보처리방침에 들어갑니다 |
| `unlockUrl` + `unlockHashes` | `node tools/make-codes.mjs 200` → 코드는 판매처에, 해시만 config에 |

### 쿠팡 파트너스 (가입 심사 통과 후)

`coupangPartnerId` 를 채우면 결과 지표에 맞춘 추천 블록과 법정 대가성 문구가 함께 켜집니다.
문구는 본문보다 큰 글씨·강조색·테두리로, 제휴 링크보다 위에, 두 곳(첫 화면과 광고 블록)에 나옵니다.

> 파트너스 **최종 승인**은 누적 판매 15만 원 이상에서 채널 심사를 거칩니다.
> 그때 "모든 활동 페이지에 경제적 이해관계 문구가 있는가"를 봅니다. 이미 그렇게 되어 있습니다.

### 애드센스 (커스텀 도메인을 산 뒤에)

`*.github.io` 로는 **승인이 나지 않습니다.** 부모 도메인 소유를 확인할 수 없고 `ads.txt` 를
루트에 올릴 수도 없기 때문입니다. 순서는 이렇습니다.

1. 도메인 구입 → `CNAME` 파일 생성 → DNS 설정
2. `assets/config.js` 의 `siteUrl` 채우고 `node tools/build-pages.mjs`
   (canonical·og:image가 절대 주소가 되고 `sitemap.xml` 이 생깁니다)
3. `ads.txt` 주석을 풀고 본인 pub 번호 입력
4. `adsenseClient` + `adsenseSlotTop` / `adsenseSlotBottom` 채우기
5. 신청

심사가 확인하는 것들은 이미 있습니다 — 개인정보처리방침(모든 페이지 푸터에서 링크),
소개·문의 페이지, 자판 소개 서브페이지 3종.

> 광고를 켜면 개인정보처리방침에 쿠키·제3자 제공·국외 이전 조항이 **자동으로** 들어갑니다.
> `contactEmail` 이 비어 있으면 빌드가 경고합니다 — 보호책임자 연락처는 법정 기재사항입니다.

---

## 3. 알리기

**초기 트래픽은 검색이 아니라 커뮤니티에서 옵니다.** 새 도메인이 구글에서 자리 잡는 데는
몇 달이 걸립니다. 그 사이의 유입도, 순위를 밀어 올릴 링크도 전부 커뮤니티에서 나옵니다.

### 원칙 다섯

1. **글의 본문이 링크가 아니라 발견이어야 합니다.** "제가 만든 도구입니다"로 시작하지 말고
   "세벌식이 이동거리는 오히려 36% 길더라"로 시작하세요. 링크는 근거로 맨 아래.
2. **표를 본문에 그대로 붙이세요.** 클릭하지 않아도 읽을 게 있어야 합니다.
3. **만든 사람임을 밝히세요.** 숨기면 나중에 그 커뮤니티를 영구히 잃습니다.
4. **한 커뮤니티에 한 번, 각도를 달리해서.** 같은 글 도배는 금물입니다.
5. **"계산이 틀렸다"는 지적에 끝까지 답하세요.** 세벌식 커뮤니티에는 30년 쓴 사람들이
   있고, 그들이 맞을 가능성이 높습니다. 고치고, 고쳤다고 다시 알리세요.
   **이게 가장 강력한 배포입니다.**

### 어디에, 어떤 각도로

| 우선 | 곳 | 각도 |
|---|---|---|
| 1 | 세벌식 사랑 모임 (다음 카페) | "390과 최종의 실제 차이를 재 봤습니다. 배열은 libhangul에서 뽑았는데 틀린 곳 알려주세요" — 여기가 원류입니다. 먼저 읽고, 겸손하게 |
| 2 | 긱뉴스 (news.hada.io) | "자판표를 원본 소스에서 기계 추출해 CI로 대조합니다 · 서버 없는 정적 사이트" — 기술적 결정이 후크 |
| 3 | 클리앙 모두의공원 | "세벌식 글이 종종 올라와서 궁금해서 만들어 봤습니다" — 정보 밀도를 높게 |
| 4 | 기계식 키보드 커뮤니티 | "영문 배열 지표(SFB·홈 유지율)를 한글에 적용하면" — 이 사람들은 개념을 이미 압니다 |
| 5 | 기존 스레드에 댓글 | 새 글보다 안전하고 효과도 좋습니다 |
| 6 | **한글날 (10월 9일)** | 연중 최고의 타이밍. 2주 전부터 준비하세요 |

### 게시글 초안 (수치는 실제 계산값입니다)

```
제목: 세벌식이 두벌식보다 손가락 이동거리가 36% 더 길다

두벌식이냐 세벌식이냐 이야기가 나올 때마다 "손가락이 덜 움직인다"가
근거로 나오는데 실제로 잰 자료를 못 찾아서 직접 만들어 봤습니다.

카톡 대화체 59음절 기준입니다.

               이동거리   타건   시프트   SFB     숫자행
  두벌식         3.13m    196     2     3.70%    0.0%
  세벌식 390     4.27m    196     3     0.74%    9.2%
  세벌식 최종     4.27m    196     3     0.74%    9.2%

세벌식이 같은 손가락 연속에서는 크게 이기는데(3.70% → 0.74%)
이동거리는 36% 더 깁니다. 자모를 숫자행까지 펼쳐 놓은 값입니다.

자판표는 libhangul 원본(hangul/hangulkeyboard.h)에서 기계로 추출했고
CI가 매번 원본을 내려받아 대조합니다. 손으로 옮겨 적은 값은 없습니다.
이동거리 모형과 그 한계는 별도 페이지에 적어 뒀습니다.

서버가 없어서 붙여 넣은 글은 브라우저 밖으로 안 나갑니다.
본인 카톡 내보내기 파일로도 재 볼 수 있습니다.

  <링크>
  소스: <GitHub 링크>

틀린 부분 있으면 알려주세요. 특히 세벌식 쪽은 제가 실사용자가 아니라
관행을 놓쳤을 수 있습니다.
```

핵심: **제목이 주장이고, 본문에 숫자가 있고, 마지막이 링크가 아니라 질문입니다.**

### 하지 말 것

- 블로그 대량 포스팅 / 지식iN 자문자답 — 어뷰징으로 잡히면 도메인이 죽습니다
- 유료 백링크 — 이 사이트의 가장 큰 자산은 정직함입니다. 팔 이유가 없습니다
- 커뮤니티 첫 글이 자기 링크
- **"세벌식이 무조건 낫다" 류의 주장** — 30년 된 논쟁에 새 사람이 판정을 내리면
  양쪽 모두에게 배척당합니다. 우리는 재기만 합니다

---

## 4. 검색으로 노릴 자리

조사해 보니 수요가 큰 질문들의 공통점이 있었습니다. **답이 숫자여야 하는데
지금 검색 1페이지에 숫자가 없습니다.** 전부 서술형 문서와 주관 후기입니다.

| 키워드 | 현재 1페이지 | 우리 자리 |
|---|---|---|
| 두벌식 세벌식 차이 | 나무위키·위키백과·개인 블로그 (전부 서술형) | **큼** → `compare.html` |
| 세벌식 390 최종 차이 | 나무위키, 글걸이 (깊지만 수치 없음) | **큼** → `compare.html` 의 13자리 표 |
| 키보드 손가락 이동거리 | 영문 도구들뿐 | **매우 큼** — 한국어 계산기가 없음 |
| 세벌식 장단점 / 후기 | 전부 주관 후기 | **큼** |
| 세벌식 배열 | 나무위키, 카페 이미지 글 | 중간 → 텍스트로 검색·복사되는 배열표 3종 |

노리지 않는 것: `세벌식`, `한글 자판`, `타자 연습` — 백과사전과 기존 강자에게 집니다.
다만 이들로 들어온 사람이 착지할 서브페이지는 이미 있습니다.

자세한 조사 원문은 `content/seo.md`, `content/monetization.md`, `content/copy.md` 에 있습니다.
(사이트에 배포되는 내용이 아니라 참고 자료입니다.)
